# ACO-VRP Optimization

This project implements an Ant Colony Optimization (ACO) algorithm to solve a Vehicle Routing Problem with Time Windows (VRPTW).

## 🚀 Features
- Multi-vehicle ACO with vehicle capacity and time windows.
- Clients generated randomly (with depot).
- Distance matrix based on Euclidean distance.
- Visual output of optimal routes using `matplotlib`.

## 📦 Requirements
Install the dependencies using:

```bash
pip install -r requirements.txt
